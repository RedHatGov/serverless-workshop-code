// author: @dudash
// license: Apache 2.0
import React from "react";
import { observer } from "mobx-react";

function Chat({ stateStore }) {    
    return (
        <div>
        </div>
    );
}
export default observer(Chat);